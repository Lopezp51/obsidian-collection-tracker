views:
  - type: table
    name: Tabela
    filters:
      and:
        - note["Equipe Criativa Geral"].isEmpty()
        - file.ext == "md"
    sort:
      - property: file.name
        direction: ASC
